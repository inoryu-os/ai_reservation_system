# 環境変数のサンプルファイル
# このファイルをコピーして `.env` を作成してください:
#   cp .env.sample .env

# --- 必須 ---
# データベース接続URL
# 開発ではSQLiteを既定にしています。
DATABASE_URL=sqlite:///meeting_rooms.db

# OpenAI APIキー（AIチャット機能で使用）
OPENAI_API_KEY=your_openai_api_key_here

# --- 推奨/任意 ---
# Flask実行モード: development / production
FLASK_ENV=development

# Flaskのセッション等で使用する秘密鍵
SECRET_KEY=change_this_secret_key

# アプリ/サーバーのタイムゾーン（JST）
TZ=Asia/Tokyo

# --- Redis設定（AIチャット履歴管理）---
# Redisサーバーのホスト
REDIS_HOST=localhost

# Redisサーバーのポート
REDIS_PORT=6379

# Redis データベース番号
REDIS_DB=0

# Redisパスワード（設定していない場合は空欄）
REDIS_PASSWORD=

# チャット履歴の有効期限（秒）デフォルト: 86400 (24時間)
CHAT_HISTORY_TTL=86400

# --- Azure OpenAI を使う場合（任意・コメント解除して使用）---
# AZURE_OPENAI_KEY=your_azure_key
# AZURE_OPENAI_ENDPOINT=https://your-resource.openai.azure.com/
# AZURE_OPENAI_API_VERSION=2024-02-15-preview
# AZURE_OPENAI_DEPLOYMENT_NAME=your-gpt-35-turbo-deployment

# --- 本番向けPostgreSQLの例（任意）---
# DATABASE_URL=postgresql://user:password@localhost/meeting_rooms

